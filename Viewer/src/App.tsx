import './App.css'

import styled, {css} from "styled-components";

const AppBlock = styled.div`
  width: 512px;
  margin: 0 auto;
  margin-top: 4rem;
  border: 1px solid black;
  padding: 1rem;
`;

const App: React.FC = () => {
    <div>
        <AppBlock>

        </AppBlock>
    </div>
}

export default App
